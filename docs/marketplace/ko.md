# Amazing Claude Code GUI

**JetBrains IDE 안에서 Claude Code를 제대로 된 채팅 패널로.** 터미널 스크롤 대신 카드로, 경로를
타이핑하는 대신 파일을 가리키는 방식으로, 그것도 코드 바로 옆에서.

이미 설치되어 있는 Claude Code CLI를 그대로 구동합니다. 계정, 모델, 슬래시 명령, 권한 규칙, MCP
서버, 스킬이 전부 그대로 따라옵니다. 중간에 프록시도 없고, 저희 계정을 만들 필요도 없습니다.

🌐 [English](en.md) | [简体中文](zh.md) | [Русский](ru.md) | [Español](es.md) | [Português (Brasil)](pt.md) | [Deutsch](de.md) | [Français](fr.md) | [日本語](ja.md) | **한국어**

## 왜 이 플러그인인가

- **파일은 타이핑하지 말고 가리키세요.** 끌어다 놓기, `@`를 입력해 고르기, 스크린샷 붙여넣기 -
  전부 오타가 날 수 없는 칩으로 들어갑니다.
- **코드는 주소와 함께 보냅니다.** 줄을 선택하고 "Send to Amazing Claude Code GUI"를 누르면,
  에이전트는 맥락 없는 조각이 아니라 그 주변의 실제 파일을 읽습니다.
- **답변의 어느 부분이든 손잡이가 됩니다.** 다음 메시지에 인용하거나, 바로 그 지점에서 대화를
  분기하세요. 원래 대화는 그대로 남습니다.
- **무엇을 하는 중인지 보입니다.** 소요 시간이 붙은 도구 호출, 증감 줄 수가 붙은 diff, 하나씩
  지워지는 할 일 목록, 계획, 서브에이전트, 워크플로 호출 하나에 딸린 여러 에이전트, 그리고 이번
  턴의 비용까지.
- **설명 없는 침묵이 없습니다.** API가 과부하이거나 한도에 걸리면 이유와 재시도 횟수, 다음 시도까지
  남은 시간이 카드로 표시됩니다.
- **아무도 대신 대답하지 않습니다.** 권한 요청도, 계획도, 질문도 필요한 만큼 기다립니다. 시간 제한도
  자동 진행도 없습니다.
- **에디터 탭이 아니라 사이드 패널**이며, 창의 어느 가장자리에나 붙일 수 있습니다.
- **대화는 패널보다 오래 삽니다.** 접어두고 다른 프로젝트에 다녀와도 에이전트는 계속 일하고 있고,
  대기 중이던 메시지도 그대로 줄 서 있습니다.
- **모델, 사고 강도, 권한 모드는 대화 도중에 바뀝니다.** 탭마다 따로, 아무것도 재시작하지 않고요.
- **휴대폰에서 답할 수 있습니다.** 기본값은 꺼짐, QR 코드로 페어링, 종단 간 암호화, 한 번의 탭으로
  해지.
- **Android Studio도 지원**하며, 2026.1 이상의 모든 JetBrains IDE에서 동작합니다.

## 시작하기

1. Claude Code를 설치하고 터미널에서 동작하는지 확인하세요. 패널이 구동하는 것이 바로 그 CLI입니다.
2. 사이드바의 버튼으로 패널을 엽니다. 로그인이 되어 있지 않다면 버튼 하나로 IDE 내장 터미널에서
   처리됩니다.
3. 메시지를 씁니다. 파일이나 폴더는 입력창에 끌어다 놓고, `@`는 프로젝트 파일, `/`는 명령, `!`는 내
   셸에서 실행입니다.
4. 에디터에서 코드를 선택하고 "Send to Amazing Claude Code GUI"를 고르면, 붙여넣은 텍스트가 아니라
   파일과 줄을 정확히 가리키는 참조가 전달됩니다.
5. 입력창 아래의 세 버튼이 모델, 사고 강도, 권한 모드이며, 각각 지금 보고 있는 탭에만 적용됩니다.

## 패널에 더 있는 것

- 터미널에서 시작한 것까지 포함한, 이 프로젝트의 지난 대화 **기록**.
- 턴이 도는 동안 쓴 메시지를 담아두는 **대기열** - 끌어서 순서를 바꿀 수 있습니다.
- **프롬프트 다듬기** - 반짝임 버튼이 초안을 별도의 실행으로 고쳐 씁니다. 대화의 컨텍스트는 쓰지
  않으며, 버튼 하나로 원래 쓴 말로 되돌아갑니다.
- 직접 발급한 Deepgram 키로 쓰는 **음성 입력** - 단축키를 누르고 있으면 되고, 에디터에서도 됩니다.
- 소리를 낼 만한 일곱 순간을 위한 **알림음** - 그것도 이미 보고 있지 않을 때만.
- 시간과 습관, 업적을 모은 **통계** - 이미지로 공유할 수 있습니다.
- **아홉 개 언어**, 기본값은 IDE를 따라갑니다.
- **저장하지 않은 편집 내용**은 턴 시작 전에 디스크에 기록되고, 에이전트가 바꾼 파일은 IDE가 곧바로
  다시 읽습니다.

## 개인정보와 투명성

- **모든 것이 내 컴퓨터에서 돌아갑니다.** 중간에 프록시도, 저희 서버도 없습니다. Claude 로그인은
  CLI의 것이고, 플러그인이 그것을 읽거나 디스크에서 API 키를 뒤지는 일은 없습니다.
- **텔레메트리도, 분석도, 계정도 없습니다.** 원격 접속이 꺼져 있으면 밖으로 나가는 것은 직접 쓰고
  보내는 피드백뿐이며, 보내기 전에 그 전문을 버튼 하나로 확인할 수 있습니다.
- **권한 규칙은 계속 당신 것입니다.** 무엇을 물어볼지는 당신의 설정과 규칙, 훅에 따라 CLI가
  정합니다. 플러그인은 자체 훅을 얹지 않고, 화면에 보이는 것보다 느슨한 모드로 세션을 시작하지도
  않습니다.
- **소스 공개** - GitHub, Elastic License 2.0. 컴퓨터를 떠날 수 있는 모든 것은
  [개인정보 처리방침](https://relay.mzpizote.com/privacy)에 적혀 있습니다.

## 요구 사항

설치 후 로그인된 Claude Code, 그리고 2026.1 이상의 JetBrains IDE(Android Studio 포함). Android
Studio에는 내장 브라우저가 없어서, IDE가 JetBrains의 브라우저 플러그인을 이 플러그인과 함께 설치할지
물어봅니다.

## 링크

- [소스 코드](https://github.com/crmapache/amazing-claude-code)
- [버그 신고 및 기능 요청](https://github.com/crmapache/amazing-claude-code/issues) - 패널 안의
  양식으로도 보낼 수 있습니다
- [개인정보 처리방침](https://relay.mzpizote.com/privacy)
